# AGENTS.md

## Dev Server

**Do not run the dev server.** The user manages it themselves.

When you need the dev server running to see your changes, tell the user:

```
npm run dev
```

The dev server supports HMR, so file changes will reflect in the browser automatically.
